#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - Computes absolute value of an int
 *@n: operand
 *
 * Return: computed value
 */

int _abs(int n)
{
	return (abs(n));
}
