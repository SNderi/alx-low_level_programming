#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and prints the result
 *@res: total of summation
 *
 * Return: 0 as success value
 */

int add(int, int)
{
	int res;

	res = int + int;
	printf("%d", res);
	return (0);
}
