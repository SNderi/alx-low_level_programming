#include <stdio.h>

/**
 * main - Prints decimal numbers 0 - 9
 *
 * Return: 0 as success value
 */

int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
