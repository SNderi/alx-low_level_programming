#include "main.h"
#include <ctype.h>

/**
 * _islower - Checks if a letter islower or not
 *
 * Return: 0 as success value
 */

int _islower(int c)
{
	if _islower(c) = 0
		_putchar(0 + '0');
	else
		_putchar(1 + '0');
	_putchar('\n');
	return (0);
}
